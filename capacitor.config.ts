import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'denarius',
  appName: 'denarius',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
